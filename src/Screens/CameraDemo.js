import React, {Component} from 'react';
import {View, StyleSheet, Text, Images} from 'react-native';

export default class CameraDemo extends Component {
  constructor(props) {
    super(props);
    this.state = {
      photo: '',
    };
  }

  render() {
    return <View></View>;
  }
}
